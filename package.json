{
  "name": "ava",
  "version": "1.0.0",
  "private": true,
  "bin": "./packages/ava/bin/cli",
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "scripts": {
    "test": "yarn workspaces run test",
    "ava": "yarn workspace @auser/ava",
    "container": "yarn workspace @auser/container",
    "kube": "yarn workspace @auser/avakube"
  },
  "license": "MIT",
  "packageManager": "yarn@3.2.0",
  "devDependencies": {
    "@types/dotenv": "^8.2.0",
    "typescript": "^4.6.2"
  },
  "dependencies": {
    "dotenv": "^16.0.0"
  }
}
