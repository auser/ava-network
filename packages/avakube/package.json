{
  "name": "@auser/avakube",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "clean": "rm -rf generated/ dist/",
    "gen": "jk generate -v -i . -o generated dist/index.js",
    "compile": "tsc -p .",
    "build": "npm run clean && npm run compile && npm run gen"
  },
  "dependencies": {
    "@dpu/jkcfg-k8s": "^0.5.4",
    "@jkcfg/kubernetes": "^0.6.2",
    "@jkcfg/std": "^0.4.0",
    "dotenv": "^16.0.0"
  },
  "devDependencies": {
    "@types/dotenv": "^8.2.0",
    "typescript": "^4.6.2"
  }
}
