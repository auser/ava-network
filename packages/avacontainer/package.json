{
  "name": "@auser/container",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "docker-compose": "docker-compose -f ./docker-compose.yml",
    "build": "npm run docker-compose build",
    "up": "npm run docker-compose up",
    "clean": "rm -rf ./dist"
  }
}
